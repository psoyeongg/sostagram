import { connect } from 'react-redux';
import Container from './container';

// Add all the actins for:
// Log in
// Sign up
// Recover password
// check username
// check password
// check email

export default connect()(Container);